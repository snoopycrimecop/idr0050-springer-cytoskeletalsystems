---
name: idr0050-springer-cytoskeletalsystems
version: 1

defaults:
  # Should the column be processed when creating bulk-annotations (yes/no)
  include: no
  # Columns type of the bulk-annotations column
  type: string

  # If non-empty a string used to separate multiple fields in a column
  # White space will be stripped
  split: ;
  # Should this column be included in the clients (yes/no)
  includeclient: yes
  # Should this column be visible in the clients, if no the column should be
  # hidden in the client but will still be indexed by the searcher (yes/no)
  visible: yes
  # Should empty values be omitted from the client display
  omitempty: yes

columns:

  - name: Channels
    clientname: Channels
    include: yes
  - name: Characteristics [Organism Part]
    clientname: Organism Part
    include: yes
  - name: Assay Name
    clientname: Assay Name
    include: yes
  - name: Experimental Condition [stretch duration]
    clientname: stretch duration
    include: yes
  - name: Comment [Image File Comments]
    clientname: Image File Comments
    include: yes
  - name: Comment [Image File Type]
    clientname: Image File Type
    include: yes

  ######################################################################
  # mapr groups
  ######################################################################


  - group:
      namespace: openmicroscopy.org/mapr/organism
      columns:
        - name: Characteristics [Organism]
          clientname: Organism
          include: yes

advanced:
  ignore_missing_primary_key: yes
  primary_group_keys:
    - namespace: openmicroscopy.org/mapr/organism
      keys:
        - Organism
